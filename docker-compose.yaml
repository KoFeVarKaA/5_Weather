networks:
  dev:
services:
  postgres:
    image: postgres:14.3
    environment:
      POSTGRES_DB: "main"
      POSTGRES_USER: "root"
      POSTGRES_PASSWORD: "password"
    ports:
      - "5434:5432"
    networks:
      - dev